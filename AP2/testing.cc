#include <iostream>
using std::cout;
using std::cin;
using std::endl;
#include "./program2functions.h"

int main(){
    int month1;
    cin >> month1;
    cout << LastDayOfMonth(month1);
    return 0;
}